server:
  port: 9003

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hms_billing_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

clients:
  bookingServiceBaseUrl: http://localhost:9002

management:
  endpoints:
    web:
      exposure:
        include: health,info

